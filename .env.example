DATABASE_URL=postgresql://maistorage:maistorage@127.0.0.1:5432/maistorage
# Local Compose default. Replace it before any non-local deployment.
POSTGRES_PASSWORD=maistorage
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5
# EMBEDDING_CACHE_DIR=.cache/fastembed

# Use newly created keys and keep this template free of credentials.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash

# Optional OpenAI-compatible endpoint for ambiguous-query routing.
# LLM_BASE_URL=http://localhost:11434/v1
# LLM_MODEL=qwen3:8b
# LLM_API_KEY=local
